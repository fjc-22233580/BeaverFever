/**
 * The Direction enum represents the possible directions that the beaver can move.
 * It includes Up, Down, Left, and Right.
 */
public enum Direction {    
    Up,
    Down,
    Left,
    Right,
}
